<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.javagreenS_lhs.dao.GenreDAO">
	<select id="genreList" resultType="com.spring.javagreenS_lhs.vo.UploadVO">
			select * from newUpload where genre = #{genre} order by idx desc limit #{startIndexNo},#{pageSize}; 
	</select>
	
	<select id="totRecCnt" resultType="int">
		select count(*) from newUpload where genre = #{genre};
	</select>
</mapper>